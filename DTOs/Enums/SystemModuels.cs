namespace DTOs.Enums
{
    public enum SystemModules
    {
        SuperAdmin = 1,
        Admin = 2,
        Provider = 3,
        Member = 4,
        Shared = 5 // this is a shared and public one
    }
}
